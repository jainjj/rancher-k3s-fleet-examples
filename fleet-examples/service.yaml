apiVersion: v1
kind: Service
metadata:
  name: hello-world
  namespace: default
spec:
  type: nodePort
  selector:
    app: hello-world
  ports:
    - name: web
      port: 80
      targetPort: 
      nodePort: 30001
